using System;

namespace Hydra
{
    enum FontName { 
        Default, 
        SpriteFont,
        kenpixel
    }

    enum FontSize {
        Default,
        size8 = 8,
        size16 = 16,
        size24 = 24,
        size32 = 32
    }
}
